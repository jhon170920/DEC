import React from 'react';
import { View, Text } from 'react-native';

const WebMapSimple = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Mapa (placeholder)</Text>
    </View>
  );
};

export default WebMapSimple;